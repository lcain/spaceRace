var app = app || {};

app.createLevelFive = function() {
    app.player.loadTexture("shipLevel5", 0, false);
    app.bulletVelocity = 400;

    app.createAliensLevelFive();
    app.stateText.visible = false;
    app.levelCounter ++;
    // app.preloadLevelSix();

}

app.createAlienGroupTopBottom = function(x, y, num, alienMove) {

    var alienArray = [];

    for (var i = 0; i < num; i++) {

        var alien = app.aliens.create(_.random(100, x), y, 'asteroidLargeLevelFour');
        alien.anchor.setTo(0.5, 0.5);
        // alien.animations.add('fly', [ 0, 1, 2, 3 ], 20, true);
        // alien.play('fly');
        alien.body.moves = false;
        alienArray.push(alien);
    }

    _.each(alienArray, function(alien){

        setTimeout(function() {
            app.game.time.events.loop(Phaser.Timer.SECOND * .01, alienMove, this, alien);
        }, _.random(1000, 25000) );

    });

}

app.createAlienGroupLeftRight = function(x, y, num, alienMove) {

    var alienArray = [];

    for (var i = 0; i < num; i++) {

        var alien = app.aliens.create(x, _.random(100, y), 'asteroidLargeLevelFour');
        alien.anchor.setTo(0.5, 0.5);
        // alien.animations.add('fly', [ 0, 1, 2, 3 ], 20, true);
        // alien.play('fly');
        alien.body.moves = false;
        alienArray.push(alien);
    }

    _.each(alienArray, function(alien){

        setTimeout(function() {
            app.game.time.events.loop(Phaser.Timer.SECOND * .01, alienMove, this, alien);
        }, _.random(1000, 25000) );

    });

}

app.createAliensLevelFive = function() {

    // Reset aliens position
    app.aliens.x = 0;
    app.aliens.y = 0;

    asteroidNum = 15;
    num = asteroidNum / 3;

    // Asteroid group moving from top to bottom
    app.createAlienGroupTopBottom(700, -300, num, app.descendLevelFive);

    // Asteroid group moving from left to right
    app.createAlienGroupLeftRight(-300, 550, num, app.moveRightLevelFive);

    // Asteroid group moving from right to left
    app.createAlienGroupLeftRight(1200, 550, num, app.moveLeftLevelFive);

}

app.descendLevelFive = function(alien) {

    alien.position.y += 1;

    if (alien.worldPosition.y > app.game.world.bounds.bottom) {
        alien.kill();
    }

}

app.moveRightLevelFive = function(alien) {

    alien.position.x += 1;

    if (alien.worldPosition.x > app.game.world.bounds.right) {
        alien.kill();
    }

}

app.moveLeftLevelFive = function(alien) {

    alien.position.x -= 1;

    if (alien.worldPosition.x < app.game.world.bounds.left) {
        alien.kill();
    }
}

app.collisionHandlerLevelFive = function(bullet, alien) {
    app.totalKillCount += 1;


    //  When a bullet hits an alien we kill them both
    bullet.kill();
    alien.kill();

    //AUDIO enemy is hit by bullet -K

    soundCall = new Howl({
    urls: [playSound("enemyHit")]
    }).play();

    //  Increase the score
    app.score += 20;
    app.scoreText.text = app.scoreString + app.score;

    //  And create an explosion :)
    var explosion = app.explosions.getFirstExists(false);
    explosion.reset(alien.body.x, alien.body.y);
    explosion.play('kaboom', 30, false, true);


}

app.preloadLevelSix = function(){

    app.game.load.spritesheet('levelSixAsteroidLarge', '<%= image_path("LevelSix/asteroidLarge.png") %>');
    app.game.load.spritesheet('levelSixAsteroidSmall', '<%= image_path("LevelSix/asteroidSmall.png") %>');
    app.game.load.spritesheet('levelSixAsteroidSplit', '<%= image_path("LevelSix/asteroidSplit.png") %>');
    app.game.load.spritesheet('levelSixBullet', '<%= image_path("LevelSix/bullet.png") %>');
    app.game.load.spritesheet('levelSixShip', '<%= image_path("LevelSix/ship.png") %>');
    app.game.load.image('levelSixBackground', '<%= image_path("LevelSix/background.png") %>');
    app.game.load.start();

};