var app = app || {};

$(document).ready(function() {

  app.game = new Phaser.Game(800, 650   , Phaser.AUTO, 'container', { preload: app.preload, create: app.create, update: app.update, render: app.render });

});

app.preload = function() {

    app.game.load.image('bullet', '<%= image_path("LevelOne/bullet.png") %>');
    app.game.load.image('enemyBullet', '<%= image_path("LevelOne/enemy-bullet.png") %>');
    app.game.load.spritesheet('invader', '<%= image_path("LevelOne/asteroid.png") %>');
    app.game.load.image('ship', '<%= image_path("LevelOne/ship.png") %>');
    app.game.load.spritesheet('kaboom', '<%= image_path("LevelOne/explode.png") %>', 128, 128);
    app.game.load.image('starfield', '<%= image_path("LevelOne/background.png") %>');


    // Preloaded images for level 3:
    app.game.load.spritesheet('asteroidLargeLevelThree', '<%= image_path("LevelThree/asteroidLarge.png") %>');
    app.game.load.spritesheet('asteroidSmallLevelThree', '<%= image_path("LevelThree/asteroidSmall.png") %>');
    app.game.load.spritesheet('asteroidSplitLevelThree', '<%= image_path("LevelThree/asteroidSplit.png") %>');


    // Preloaded images for level 4:
    app.game.load.spritesheet('shipLevel4', '<%= image_path("LevelFour/ship.png") %>');

    app.game.load.spritesheet('asteroidLargeLevelFour', '<%= image_path("LevelFour/asteroidLarge.png") %>');

    //Preloaded images for level 5:
     app.game.load.spritesheet('shipLevel5', '<%= image_path("LevelFive/ship.png") %>');


    // Preloaded images for level 6:
    app.game.load.spritesheet('levelSixAsteroidLarge', '<%= image_path("LevelSix/asteroidLarge.png") %>');
    app.game.load.spritesheet('levelSixAsteroidSmall', '<%= image_path("LevelSix/asteroidSmall.png") %>');
    app.game.load.spritesheet('levelSixAsteroidSplit', '<%= image_path("LevelSix/asteroidSplit.png") %>');
    app.game.load.spritesheet('levelSixBullet', '<%= image_path("LevelSix/bullet.png") %>');
    app.game.load.spritesheet('levelSixShip', '<%= image_path("LevelSix/ship.png") %>');
    app.game.load.image('levelSixBackground', '<%= image_path("LevelSix/background.png") %>');

    // LEAVE THIS AT THE END - JQUERY ELEMENTS AND CLASS ASIGNMENTS. - KANE
    $('body canvas').addClass("gameWindow");
    $('<div class="gameStatus">'+'<p class="scoreText gameScore">'+app.score+'</p>'+'<p class="scoreText gameLives"> 3 </p>'+'</div>').appendTo('#container');
    $('<div class="optionsBar"><div class="userButton toggleSound"><p>Mute</p></div></div>').appendTo('.gameStatus');
    $('<div class="toggleStats userButton"><p>Stats</p></div>').appendTo('.optionsBar');
    $('<div class="statsWindow hidden"><h4>Stats</h4></div>').appendTo('#container');





      // Temporarily start muted.
    Howler.mute();
    $('.toggleSound').addClass("userButtonDisable")
    $('.toggleStats').addClass("userButtonDisable")
}

app.render = function() {

    // for (var i = 0; i < aliens.length; i++)
    // {
    //     game.debug.body(aliens.children[i]);
    // }

}